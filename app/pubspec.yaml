name: sach_noi
description: "Sách lười — chuyển sách EPUB/TXT thành sách nói tiếng Việt"
publish_to: "none"
version: 1.5.4+22

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # Phát âm thanh: media_kit dùng libmpv, ổn định trên Windows và có sẵn
  # đường lên Android/iOS sau này.
  media_kit: ^1.2.0
  media_kit_libs_windows_audio: ^1.0.9
  media_kit_libs_android_audio: ^1.3.7
  media_kit_libs_macos_audio: ^1.1.4
  media_kit_libs_ios_audio: ^1.1.4

  # Đọc EPUB (là file zip) và chọn file từ máy
  archive: ^4.0.7
  file_picker: ^10.3.4

  # Lưu trữ, mã hoá, tải mô hình
  path: ^1.9.1
  path_provider: ^2.1.5
  crypto: ^3.0.6
  http: ^1.5.0
  ffi: ^2.2.0
  # XInput để đọc tay cầm chơi game trên Windows (xem services/tay_cam_windows.dart)
  win32: ^5.15.0
  audio_service: ^0.18.19
  sherpa_onnx: ^1.13.4
  scrollable_positioned_list: ^0.3.8
  flutter_tts: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # Từ điển âm vị tiếng Việt và hồ sơ hai giọng nhân bản. Đi kèm ứng dụng vì
    # không có nguồn tải công khai ổn định; mô hình thì tải riêng lúc chạy.
    - assets/sea_g2p.bin
    - assets/giong.json
